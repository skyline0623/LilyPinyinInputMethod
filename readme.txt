可运行的jar包包含在文件夹中，需要一个java运行环境。
文件夹中亦包含了源代码，使用的语料库和拼音汉字对照表并未包含其中，源码在Eclipse或者其他IDE中可以直接运行，入口是CInputMethod.java。
用户在上面的输入框输入拼音（可以是错误的拼音），然后下边出现候选名单，默认选择5个汉字，点击键盘1~5可以选择汉字，这样汉字就出现在下边的文本框中。
点击键盘6或者鼠标点击后一页可以向下翻页，点击键盘~或者鼠标点击前一页可以向上翻页。点击键盘空格默认选择第一个汉字。

